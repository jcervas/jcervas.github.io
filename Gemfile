source "https://rubygems.org"

# Matches GitHub Pages' build environment so local previews match production.
# Local preview:  bundle install  &&  bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

# Lock a compatible webrick (needed for local serving on Ruby 3+)
gem "webrick", "~> 1.8"
